class LolRank {
  String ranking;
  int rank;
  String profile;
  String userName;
  String name;
  int grade;
  int classes;

  LolRank(this.ranking, this.rank, this.profile, this.userName, this.name,
      this.grade, this.classes);
}
