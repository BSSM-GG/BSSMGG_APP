class ValRank {
  String ranking;
  int rank;
  String profile;
  String userName;
  String name;
  int grade;
  int classes;

  ValRank(this.ranking, this.rank, this.profile, this.userName, this.name,
      this.grade, this.classes);
}
